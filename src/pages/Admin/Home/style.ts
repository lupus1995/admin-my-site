import { createUseStyles } from "react-jss";

const useStyles = createUseStyles({
  pageTitle: {
    padding: "10px 16px",
  },
  homeForm: {
    width: "1000px",
  },
  homeRow: {
    marginBottom: "15px",
  },
});

export default useStyles;
